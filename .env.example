# Local validation only. There are no production secrets for this service --
# babel is text in, text out, with no API keys and no state worth protecting.

# Where the ~8.7 GB model cache lives locally. Keep it off the root filesystem.
LT_STORAGE_ROOT=
